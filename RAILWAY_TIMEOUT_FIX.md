# Railwayデプロイタイムアウトエラー対応

## 🔧 問題

Railwayへのデプロイ時に以下のエラーが発生する場合があります：

```
error sending request for url (https://backboard.railway.com/.../up?serviceId=...)
Caused by:
    operation timed out
```

## ✅ 解決方法

### 方法1: リトライ機能付きデプロイスクリプトを使用（推奨）

```bash
deploy-railway-retry.bat
```

このスクリプトは自動的に3回までリトライします。

### 方法2: 手動でリトライ

```bash
# デプロイを再実行
railway up --detach

# 複数回試行する場合
railway up --detach
railway up --detach
railway up --detach
```

### 方法3: Railwayダッシュボードからデプロイ

1. https://railway.app にアクセス
2. プロジェクトを開く
3. サービスを選択
4. 「Deployments」タブで「Redeploy」をクリック
5. または、GitHubリポジトリにプッシュすると自動デプロイされます

## 🔍 原因

タイムアウトエラーの主な原因：

1. **ネットワーク接続の問題**
   - インターネット接続が不安定
   - ファイアウォールやプロキシの設定

2. **Railwayサーバーの負荷**
   - Railwayのサーバーが一時的に高負荷
   - メンテナンス中

3. **大きなファイルサイズ**
   - デプロイするファイルが大きすぎる
   - node_modulesが含まれている

4. **Railway CLIのタイムアウト設定**
   - デフォルトのタイムアウト時間が短い

## 🛠️ 対処方法

### 1. ネットワーク接続を確認

```bash
# RailwayのAPIに接続できるか確認
curl https://backboard.railway.com

# Railway CLIのバージョンを確認
railway --version
```

### 2. .railwayignoreファイルを作成

大きなファイルを除外してデプロイサイズを削減：

```bash
# .railwayignoreファイルを作成
echo node_modules > .railwayignore
echo .git >> .railwayignore
echo dist >> .railwayignore
echo *.log >> .railwayignore
```

### 3. 環境変数でタイムアウトを延長（可能な場合）

```bash
# 環境変数を設定（Railway CLIがサポートしている場合）
set RAILWAY_TIMEOUT=300
railway up --detach
```

### 4. 段階的なデプロイ

大きな変更を小さな単位に分割してデプロイ：

```bash
# 1. 設定ファイルのみをデプロイ
git add railway.toml Dockerfile.railway
git commit -m "Update Railway config"
railway up --detach

# 2. コードをデプロイ
git add .
git commit -m "Update code"
railway up --detach
```

## 📝 デプロイスクリプトの使い方

### deploy-railway-retry.bat（リトライ機能付き）

```bash
# 実行
deploy-railway-retry.bat

# 特徴:
# - 自動的に3回までリトライ
# - 各リトライの間に10秒の待機時間
# - エラーメッセージと対処方法を表示
```

### deploy-railway-auto.bat（標準版）

```bash
# 実行
deploy-railway-auto.bat

# タイムアウトが発生した場合は、deploy-railway-retry.batを使用
```

## 🔍 デプロイ状態の確認

```bash
# Railwayのステータスを確認
railway status

# デプロイメントのログを確認
railway logs

# デプロイメントの履歴を確認（Railwayダッシュボード）
# https://railway.app → プロジェクト → サービス → Deployments
```

## 📚 参考リンク

- [Railway Documentation](https://docs.railway.app/)
- [Railway Status](https://status.railway.app/)
- [Railway CLI Reference](https://docs.railway.app/develop/cli)
